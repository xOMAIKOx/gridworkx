{
  "name": "@gridworks/monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/admin",
    "apps/tools"
  ],
  "scripts": {
    "build": "npm run build --workspace @gridworks/admin",
    "test": "npm run test --workspace @gridworks/admin",
    "typecheck": "npm run typecheck --workspace @gridworks/admin",
    "validate": "npm run validate --workspace @gridworks/tools"
  },
  "devDependencies": {
    "ajv": "8.17.1"
  }
}

Deterministic and replay coverage lives beside the canonical Rust crate and is executed with `cargo test --workspace`. The integration harness under `crates/gridworks-sim/tests/replay.rs` proves snapshot restoration, identical event/state/digest replay and version rejection. WP-003 graph unit tests prove fixed-point bottlenecks, hard/optional/parallel semantics, cycle/reference rejection and aggregate-plant evaluation.

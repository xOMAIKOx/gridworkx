# GRIDWORKS tooling foundation

The validation tool owns repository-side schema/content smoke checks. It validates the canonical JSON Schema catalogue, semantic IDs, duplicate domain/bundle IDs and required namespace coverage without introducing a second rules engine.
